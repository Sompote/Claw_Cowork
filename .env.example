# Claw Cowork - Environment Configuration
# Copy to .env and fill in your values

# Server port (default: 3001)
PORT=3001

# Sandbox working directory (default: current directory)
SANDBOX_DIR=

# Optional access token to protect the UI (leave blank = no auth)
ACCESS_TOKEN=

# Node environment
NODE_ENV=development
